import React from "react";

const LoadingTx = () => {
    return (
        <div>
            <h4>Loading...</h4>
            {/* Tambahkan komponen atau gambar loading di sini */}
        </div>
    );
};

export default LoadingTx;